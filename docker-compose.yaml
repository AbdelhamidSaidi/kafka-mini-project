services:
  kafka:
    build: ./docker/kafka
    image: local/apache-kafka:4.2.0
    container_name: kafka
    ports:
      - "9092:9092"
    environment:
      # KRaft mode (no Zookeeper)
      KAFKA_NODE_ID: 1
      KAFKA_PROCESS_ROLES: 'broker,controller'
      KAFKA_LISTENERS: 'PLAINTEXT://:9092,CONTROLLER://:9093'
      KAFKA_ADVERTISED_LISTENERS: 'PLAINTEXT://localhost:9092'
      KAFKA_CONTROLLER_LISTENER_NAMES: 'CONTROLLER'
      KAFKA_LISTENER_SECURITY_PROTOCOL_MAP: 'CONTROLLER:PLAINTEXT,PLAINTEXT:PLAINTEXT'
      KAFKA_CONTROLLER_QUORUM_VOTERS: '1@localhost:9093'
      KAFKA_OFFSETS_TOPIC_REPLICATION_FACTOR: 1
      KAFKA_TRANSACTION_STATE_LOG_REPLICATION_FACTOR: 1
      KAFKA_TRANSACTION_STATE_LOG_MIN_ISR: 1
      KAFKA_GROUP_INITIAL_REBALANCE_DELAY_MS: 0
    volumes:
      - ./docker/kafka-data:/var/lib/kafka/data
      - ./docker/kafka-config:/opt/kafka/config:ro

  mssql:
    build: ./docker/mssql
    image: local/mssql:2019
    container_name: mssql
    environment:
      - SA_PASSWORD=${MSSQL_SA_PASSWORD}
      - ACCEPT_EULA=Y
    ports:
      - "1433:1433"
    volumes:
      - ./docker/mssql/data:/var/opt/mssql
      - ./docker/mssql/initdb:/initdb

  airflow:
    build: ./docker/airflow
    image: local/airflow-mssql:2.8.1
    container_name: airflow
    restart: always
    depends_on:
      - mssql
    ports:
      - "8080:8080"
    environment:
      - AIRFLOW__CORE__LOAD_EXAMPLES=False
      - AIRFLOW__CORE__EXECUTOR=LocalExecutor
      - AIRFLOW__DATABASE__SQL_ALCHEMY_CONN=${MSSQL_ALCHEMY_CONN}
    volumes:
      - ./dags:/opt/airflow/dags
      - ./airflow_db:/opt/airflow
      - ./:/opt/airflow/workspace:cached
    command: >
      bash -c "
        airflow db upgrade &&
        airflow users create \
          --username admin \
          --password admin \
          --firstname admin \
          --lastname user \
          --role Admin \
          --email admin@example.com || true &&
        airflow webserver &
        airflow scheduler
      "

volumes:
  kafka_data:
